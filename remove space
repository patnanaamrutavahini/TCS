#include <bits/stdc++.h>
using namespace std;
int main() {
    // Write C++ code here
    string s;
    getline(cin,s);
    string result="";
    for(int i=0;i<s.length();i++){
        if(s[i]!=' '){
            result+=s[i];
        }
    }
    cout<<result;
    return 0;
}

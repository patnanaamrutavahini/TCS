#include <bits/stdc++.h>
using namespace std;
int main() {
    // Write C++ code here
    int n,key;
    cin>>n;
    cin>>key;
    vector<int>a(n);
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    for(int i = 0; i < n; i++) {
        if(a[i] == key) {
            cout << "Found at position " << i + 1;
            return 0;
        }
    }

    cout << "Element not found";
}

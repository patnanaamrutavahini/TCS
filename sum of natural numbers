#include <bits/stdc++.h>
using namespace std;

int f(int n) {
    if(n<=0) return 0;
    if(n==1) return 1;
    int sum=0;
    for(int i=0;i<=n;i++){
        sum=sum+i;
    }
    return sum;
}
int main() {
    int n;
    cin >> n;
    cout<<f(n);
    return 0;
}
